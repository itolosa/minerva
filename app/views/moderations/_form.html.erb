<%= form_for([@course, moderation]) do |f| %>
  <% if moderation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(moderation.errors.count, "error") %> prohibited this moderation from being saved:</h2>

      <ul>
      <% moderation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.text_field :course_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
